# editor-crdt-example

A CRDT collaborative editor demo built with React 18, Yjs, and Vite. Demonstrates concurrent text editing with conflict-free replicated data types.

## Quick Start

Install dependencies:

```bash
npm ci || npm install --no-fund --prefer-offline
```

Run the dev server:

```bash
npm run dev
```
